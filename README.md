# Encoder App
An open-source multi-media transcoder app
## Build Instructions
### macOS or Linux
```bash
mkdir build && cd build
cmake ..
make -j"$(nproc)"
```
### Windows
Not supported yet. Windows build support will be added before the final release.